package com.example.autoglazecustomer.ui

import androidx.compose.runtime.Composable

@Composable
actual fun KmpBackHandler(enabled: Boolean, onBack: () -> Unit) {
    // Kosong karena iOS tidak punya tombol back sistem
}